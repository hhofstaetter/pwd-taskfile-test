---
services:
  node-one-folder:
    image: node:20.16-alpine
    volumes:
      - volume-one-folder:/app
    command: ['/bin/sh', '-c', 'ls -l /app']

volumes:
  volume-one-folder:
    driver_opts:
      type: 'none'
      o: 'bind'
      device: '${PWD}'
