---
services:
  node-this-folder:
    image: node:20.16-alpine
    volumes:
      - volume-this-folder:/app
    command: ['/bin/sh', '-c', 'ls -l /app']

volumes:
  volume-this-folder:
    driver_opts:
      type: 'none'
      o: 'bind'
      device: '${PWD}'
